{
	// "image": "mcr.microsoft.com/devcontainers/ruby",

    "build": {
        // Path is relative to the devcontainer.json file.
        "dockerfile": "Dockerfile"
    },

	"customizations": {
		"vscode": {
			"extensions": ["shopify.ruby-lsp", "codeium.codeium"]
		}
	}
}